# create/activate virtual Env
   python3 -m venv venv
   source venv/bin/activate

# Run live server
  uvicorn main:app --reload
#Install all requirements.txt
   pip install -r requirements.txt

#Kill pre-existing postgress connection
   sudo lsof -i :5432 
   sudo pkill -u postgres 
   
